#include "main.h"
#include <stdio.h>
/**
 * main - prints the name
 * @argc : argument count
 * @argv : argument vector
 * Return : Always
 */
int main(int argc, char *argv[])
{

	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
