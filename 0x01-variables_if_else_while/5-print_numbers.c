#include <stdio.h>
/**
 * main - prints all numbers to the base ten
 * Return: Always  (Success)
 */

int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
	printf ("%d", x);
	printf ("\n");

	return (0);
}
